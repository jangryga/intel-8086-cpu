use crate::memory;
use crate::instruction_table;
// use std::sync::Mutex;

struct Context {
    offset: u32
}

// static CONTEXT: Mutex<Context> = Mutex::new(Context {
//     offset: 0
// });

// fn decode_instruction

fn dis_asm8086() {}